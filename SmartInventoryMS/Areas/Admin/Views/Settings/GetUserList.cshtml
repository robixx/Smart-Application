@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "User List";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="dashboard-content">
    <h1 class="page-title">User Management</h1>

    <div class="table-card">
        <div class="table-header">
            <h3 class="table-title">All Users</h3>
            <button class="btn-add-new" onclick="openModal()">
                <i class="bi bi-plus-lg"></i>
                Add New User
            </button>
        </div>
        <div class="table-responsive">
            <table id="usersTable" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>UserName</th>
                        <th>Email</th>
                        <th>IsActive</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>1</td>
                        <td>
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <div class="user-avatar">JD</div>
                                <span>John Doe</span>
                            </div>
                        </td>
                        <td>john.doe@example.com</td>
                        <td><span class="status-badge status-active">Active</span></td>
                        <td>
                            <button class="action-btn edit" onclick="editUser(1)">
                                <i class="bi bi-pencil-fill"></i>
                            </button>
                            <button class="action-btn delete" onclick="deleteUser(1)">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </td>
                    </tr>
                    <tr>
                        <td>2</td>
                        <td>
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <div class="user-avatar">AJ</div>
                                <span>Alice Johnson</span>
                            </div>
                        </td>
                        <td>alice.johnson@example.com</td>
                        <td><span class="status-badge status-active">Active</span></td>
                        <td>
                            <button class="action-btn edit" onclick="editUser(2)">
                                <i class="bi bi-pencil-fill"></i>
                            </button>
                            <button class="action-btn delete" onclick="deleteUser(2)">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </td>
                    </tr>
                    <tr>
                        <td>3</td>
                        <td>
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <div class="user-avatar">BS</div>
                                <span>Bob Smith</span>
                            </div>
                        </td>
                        <td>bob.smith@example.com</td>
                        <td><span class="status-badge status-inactive">Inactive</span></td>
                        <td>
                            <button class="action-btn edit" onclick="editUser(3)">
                                <i class="bi bi-pencil-fill"></i>
                            </button>
                            <button class="action-btn delete" onclick="deleteUser(3)">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </td>
                    </tr>
                    <tr>
                        <td>4</td>
                        <td>
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <div class="user-avatar">CW</div>
                                <span>Carol White</span>
                            </div>
                        </td>
                        <td>carol.white@example.com</td>
                        <td><span class="status-badge status-active">Active</span></td>
                        <td>
                            <button class="action-btn edit" onclick="editUser(4)">
                                <i class="bi bi-pencil-fill"></i>
                            </button>
                            <button class="action-btn delete" onclick="deleteUser(4)">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </td>
                    </tr>
                    <tr>
                        <td>5</td>
                        <td>
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <div class="user-avatar">DB</div>
                                <span>David Brown</span>
                            </div>
                        </td>
                        <td>david.brown@example.com</td>
                        <td><span class="status-badge status-active">Active</span></td>
                        <td>
                            <button class="action-btn edit" onclick="editUser(5)">
                                <i class="bi bi-pencil-fill"></i>
                            </button>
                            <button class="action-btn delete" onclick="deleteUser(5)">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </td>
                    </tr>
                    <tr>
                        <td>6</td>
                        <td>
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <div class="user-avatar">EG</div>
                                <span>Eva Green</span>
                            </div>
                        </td>
                        <td>eva.green@example.com</td>
                        <td><span class="status-badge status-inactive">Inactive</span></td>
                        <td>
                            <button class="action-btn edit" onclick="editUser(6)">
                                <i class="bi bi-pencil-fill"></i>
                            </button>
                            <button class="action-btn delete" onclick="deleteUser(6)">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </td>
                    </tr>
                    <tr>
                        <td>7</td>
                        <td>
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <div class="user-avatar">FM</div>
                                <span>Frank Miller</span>
                            </div>
                        </td>
                        <td>frank.miller@example.com</td>
                        <td><span class="status-badge status-active">Active</span></td>
                        <td>
                            <button class="action-btn edit" onclick="editUser(7)">
                                <i class="bi bi-pencil-fill"></i>
                            </button>
                            <button class="action-btn delete" onclick="deleteUser(7)">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </td>
                    </tr>
                    <tr>
                        <td>8</td>
                        <td>
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <div class="user-avatar">GL</div>
                                <span>Grace Lee</span>
                            </div>
                        </td>
                        <td>grace.lee@example.com</td>
                        <td><span class="status-badge status-active">Active</span></td>
                        <td>
                            <button class="action-btn edit" onclick="editUser(8)">
                                <i class="bi bi-pencil-fill"></i>
                            </button>
                            <button class="action-btn delete" onclick="deleteUser(8)">
                                <i class="bi bi-trash-fill"></i>
                            </button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Add/Edit User Modal -->
<div class="modal-overlay" id="userModal">
    <div class="modal-content">
        <div class="modal-header">
            <h3 class="modal-title" id="modalTitle">Add New User</h3>
            <button class="modal-close" onclick="closeModal()">
                <i class="bi bi-x-lg"></i>
            </button>
        </div>
        <div class="modal-body">
            <form id="userForm">
                <input type="hidden" id="userId">
                <div class="form-group">
                    <label class="form-label" for="userName">UserName</label>
                    <input type="text" class="form-control" id="userName" placeholder="Enter username" required>
                </div>
                <div class="form-group">
                    <label class="form-label" for="email">Email</label>
                    <input type="email" class="form-control" id="email" placeholder="Enter email address" required>
                </div>
                <div class="form-group">
                    <label class="form-label" for="password">Password</label>
                    <input type="password" class="form-control" id="password" placeholder="Enter password" required>
                </div>
                <div class="form-group">
                    <div class="form-check">
                        <input type="checkbox" class="form-check-input" id="isActive" checked>
                        <label class="form-check-label" for="isActive">Active</label>
                    </div>
                </div>
            </form>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn-cancel" onclick="closeModal()">Cancel</button>
            <button type="button" class="btn-submit" onclick="saveUser()">Save User</button>
        </div>
    </div>
</div>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script>
    // User data storage
    let usersData = [
        { id: 1, userName: 'John Doe', email: 'john.doe@example.com', password: '******', isActive: true },
        { id: 2, userName: 'Alice Johnson', email: 'alice.johnson@example.com', password: '******', isActive: true },
        { id: 3, userName: 'Bob Smith', email: 'bob.smith@example.com', password: '******', isActive: false },
        { id: 4, userName: 'Carol White', email: 'carol.white@example.com', password: '******', isActive: true },
        { id: 5, userName: 'David Brown', email: 'david.brown@example.com', password: '******', isActive: true },
        { id: 6, userName: 'Eva Green', email: 'eva.green@example.com', password: '******', isActive: false },
        { id: 7, userName: 'Frank Miller', email: 'frank.miller@example.com', password: '******', isActive: true },
        { id: 8, userName: 'Grace Lee', email: 'grace.lee@example.com', password: '******', isActive: true }
    ];

    // Modal functions
    function openModal() {
        document.getElementById('userModal').classList.add('show');
        document.getElementById('modalTitle').textContent = 'Add New User';
        document.getElementById('userForm').reset();
        document.getElementById('userId').value = '';
        document.getElementById('password').required = true;
    }

    function closeModal() {
        document.getElementById('userModal').classList.remove('show');
    }

    function saveUser() {
        const userId = document.getElementById('userId').value;
        const userName = document.getElementById('userName').value;
        const email = document.getElementById('email').value;
        const password = document.getElementById('password').value;
        const isActive = document.getElementById('isActive').checked;

        if (!userName || !email || !password) {
            alert('Please fill in all required fields');
            return;
        }

        if (userId) {
            // Edit existing user
            const index = usersData.findIndex(u => u.id == userId);
            if (index !== -1) {
                usersData[index] = { ...usersData[index], userName, email, isActive };
            }
        } else {
            // Add new user
            const newId = usersData.length > 0 ? Math.max(...usersData.map(u => u.id)) + 1 : 1;
            usersData.push({ id: newId, userName, email, password: '******', isActive });
        }

        closeModal();
        refreshTable();
    }

    function editUser(id) {
        const user = usersData.find(u => u.id === id);
        if (user) {
            document.getElementById('userModal').classList.add('show');
            document.getElementById('modalTitle').textContent = 'Edit User';
            document.getElementById('userId').value = user.id;
            document.getElementById('userName').value = user.userName;
            document.getElementById('email').value = user.email;
            document.getElementById('password').value = '';
            document.getElementById('password').required = false;
            document.getElementById('password').placeholder = 'Enter new password (leave empty to keep current)';
            document.getElementById('isActive').checked = user.isActive;
        }
    }

    function deleteUser(id) {
        if (confirm('Are you sure you want to delete this user?')) {
            usersData = usersData.filter(u => u.id !== id);
            refreshTable();
        }
    }

    function getInitials(name) {
        return name.split(' ').map(n => n[0]).join('').toUpperCase();
    }

    function refreshTable() {
        const table = $('#usersTable').DataTable();
        table.clear();
            
        usersData.forEach(user => {
            const statusClass = user.isActive ? 'status-active' : 'status-inactive';
            const statusText = user.isActive ? 'Active' : 'Inactive';
                
            table.row.add([
                user.id,
                `<div style="display: flex; align-items: center; gap: 12px;">
                    <div class="user-avatar">${getInitials(user.userName)}</div>
                    <span>${user.userName}</span>
                </div>`,
                user.email,
                `<span class="status-badge ${statusClass}">${statusText}</span>`,
                `<button class="action-btn edit" onclick="editUser(${user.id})">
                    <i class="bi bi-pencil-fill"></i>
                </button>
                <button class="action-btn delete" onclick="deleteUser(${user.id})">
                    <i class="bi bi-trash-fill"></i>
                </button>`
            ]);
        });
            
        table.draw();
    }

 

    // Initialize DataTable
    $(document).ready(function() {
        if ($.fn.DataTable.isDataTable('#usersTable')) {
            $('#usersTable').DataTable().destroy();
        }
            
        var usersDataTable = $('#usersTable').DataTable({
            processing: false,
            serverSide: false,
            responsive: true,
            paging: true,
            searching: true,
            ordering: true,
            info: true,
            order: [[0, 'asc']],
                
            lengthChange: true,
            pageLength: 10,
            lengthMenu: [10, 25, 50, 100],
                
            language: {
                emptyTable: "No data available in table",
                info: "Showing _START_ to _END_ of _TOTAL_ entries",
                infoEmpty: "Showing 0 to 0 of 0 entries",
                infoFiltered: "(filtered from _MAX_ total entries)",
                lengthMenu: "Show _MENU_ entries",
                loadingRecords: "Loading...",
                processing: "Processing...",
                search: "_INPUT_",
                searchPlaceholder: "Search...",
                zeroRecords: "No matching records found",
                paginate: {
                    first: '<i class="bi bi-chevron-double-left"></i>',
                    last: '<i class="bi bi-chevron-double-right"></i>',
                    next: '<i class="bi bi-chevron-right"></i>',
                    previous: '<i class="bi bi-chevron-left"></i>'
                },
                aria: {
                    sortAscending: ": activate to sort column ascending",
                    sortDescending: ": activate to sort column descending"
                }
            },
                
            dom:
                "<'row'<'col-sm-12 col-md-6'l><'col-sm-12 col-md-6'f>>" +
                "<'row'<'col-sm-12'tr>>" +
                "<'row'<'col-sm-12 col-md-5'i><'col-sm-12 col-md-7'p>>",
                
            columns: [
                { title: "ID", orderable: true, searchable: true },
                { title: "UserName", orderable: true, searchable: true },
                { title: "Email", orderable: true, searchable: true },
                { title: "IsActive", orderable: true, searchable: true },
                { title: "Actions", orderable: false, searchable: false }
            ],
                
            drawCallback: function(settings) {
                $('#usersTable').addClass('table-striped');
            },
                
            initComplete: function(settings, json) {
                $('.dataTables_length select').addClass('form-select form-select-sm');
                $('.dataTables_filter input').addClass('form-control');
                    
                $('.dataTables_filter input').css({
                    'borderRadius': '25px',
                    'padding': '8px 15px',
                    'maxWidth': '300px'
                });
                    
                $('.dataTables_length select').css({
                    'borderRadius': '8px',
                    'padding': '5px 10px'
                });
                    
                $('.dataTables_paginate').addClass('pagination-sm');
                    
                // Initialize table with existing data
                refreshTable();
                    
                console.log('DataTable initialized successfully');
            }
        });
            
        $('#usersTable').on('search.dt', function() {
            console.log('Search applied:', usersDataTable.search());
        });
    });
        
    $(window).on('resize', function() {
        if ($.fn.DataTable.isDataTable('#usersTable')) {
            $('#usersTable').DataTable().columns.adjust();
        }
    });
</script>